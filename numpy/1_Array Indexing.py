import numpy as np
lst = list(map(int,input("Enter the List :").split()))
arr = np.array(lst)
new_ar = arr.astype(String)
print(f"Numpy Array : {new_ar}")
print(f"Normal Array / List : {lst}")

# adding = arr[0] + arr[len(arr) - 1]
# print(adding)

